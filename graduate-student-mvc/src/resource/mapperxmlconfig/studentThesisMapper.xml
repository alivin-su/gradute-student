<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.d.mapper.StudentThesisMapper">
    <select id="selectThesis" resultType="com.d.bean.Thesis">
        select `id`, `title`, `type`, `author`, `year`, `check`
        from `thesis`
        where `user_thesis_id` = (select id from student where name = #{name})
    </select>
    <select id="selectThesisById" resultType="com.d.bean.Thesis">
        select `title`, `type`, `author`, `year`, `check`
        from `thesis`
        where id = #{thesisID}
    </select>
    <select id="selectThesisByPage" resultType="com.d.bean.Thesis">
        select `id`, `title`, `type`, `author`, `year`, `check`
        from `thesis`
        where `user_thesis_id` = (select id from student where name = #{param1}) limit #{param2},#{param3}
    </select>
</mapper>